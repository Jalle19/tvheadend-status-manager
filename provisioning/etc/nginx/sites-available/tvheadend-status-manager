server {
	listen 80 default_server;
	listen [::]:80 default_server ipv6only=on;

	root /vagrant/src/client;
	index index.html index.htm;
	sendfile off;

	location / {
		try_files $uri $uri/ =404;
	}
	
	gzip on;
	gzip_comp_level 9;
	gzip_types text/html text/css application/x-javascript;
}
